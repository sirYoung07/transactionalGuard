server:
    port: 8000
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/transactional-guard
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

    output:
        ansi:
            enabled: always


logging:
    file:
        name: loggers.log
